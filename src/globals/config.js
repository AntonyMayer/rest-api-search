// Should not be a part of code, using it to avoid creating login screen
const MASTER = {
    username: '',
    password: '',
};

/**
 * API Endpoints
 */
const AUTH_ENDPOINT = '';
const SEARCH_ENDPOINT = '';

// Should be a dynamic value set by user
// but for the sake of saving time using a hardcoded value
const RESULTS_PER_PAGE = 25;

export {
    MASTER,
    SEARCH_ENDPOINT,
    AUTH_ENDPOINT,
    RESULTS_PER_PAGE,
};